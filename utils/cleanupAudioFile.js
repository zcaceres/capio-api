const path = require('path')
const fs = require('fs')

// TODO: Clean up audio files after creating transcripts
module.exports = function(pathToCleanUp) {
  console.log('DELETING FILE AT', pathToCleanUp)
}
